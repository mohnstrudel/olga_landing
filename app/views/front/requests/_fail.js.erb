
var errors = "<%= @request.errors.messages %>";
var type = "<%= @js_type %>";
console.log("Type is: " + type);

console.log(typeof errors);
errors_array = errors.split(',');
for (item of errors_array) {
  var field_name = item.substring(item.lastIndexOf(":")+1, item.lastIndexOf("="));
  $input = $('form').find("[name='" + type + "[" + field_name + "]']");
  $input.closest('.form-data__parametr').toggleClass("error", true);

  // $input_callmeback = $('form').find("input[name='call_me_back_request[" + field_name + "]']");
  // $input_callmeback.parents(".g-input").toggleClass("g-input_error", true);
}